import { COMPONENTS, isComponentName, type ComponentName} from "./types/component.type";
import { DAMAGE_TYPES, isDamageType, type DamageType } from "./types/damage-type.type";
import { GROUPS, isGroupName, type GroupName } from "./types/group.type";
import { SCHOOLS, isSchoolName, type SchoolName } from "./types/school.type";
import { SOURCES, isSourceName, type SourceName } from "./types/source.type";
import { SPELL_LEVELS, isSpellLevel, type SpellLevel } from "./types/spell-level.type";

export { 
    COMPONENTS, isComponentName, ComponentName,
    DAMAGE_TYPES, isDamageType, DamageType,
    GROUPS, isGroupName, GroupName,
    SCHOOLS, isSchoolName, SchoolName,
    SOURCES, isSourceName, SourceName,
    SPELL_LEVELS, isSpellLevel, SpellLevel
};